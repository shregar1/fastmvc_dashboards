# Optional development dependencies for this package.
# Primary install: pip install -e ".[dev]" (see pyproject.toml).
# Then optionally: pip install -r requirements.txt
#
# Monorepo: python3 scripts/sync_package_tooling.py refreshes this file from the template.

pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
httpx>=0.24.0
mypy>=1.0.0
ruff>=0.1.0
bandit>=1.7.0
twine>=4.0.0
build>=0.10.0
pre-commit>=3.0.0
